import { createSelector } from "@ngrx/store";
import { ProductState } from "./product.reducer";

// export const selectProductById = (id: string) => createSelector(
//     selectProductState,
//     (state: ProductState) => state.products[id]
//   );
  